# Traffic_Light_Controller
A traffic light controller in Verilog uses a Finite State Machine (FSM). The FSM acts like a brain. It steps through states (e.g., Red, Yellow, Green). A timer module counts clock cycles to change the lights safely over time.
